name: lint

on:
  push:
    branches: ['main', 'master']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name:
        run: |
          npm install
          npm run lint
